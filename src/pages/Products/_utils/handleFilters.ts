// const handleDateChange = (e: React.ChangeEvent<HTMLInputElement> setFilter: ) => {
// 	const { name, value } = e.target;
// 	setFilters((prev) => ({
// 		...prev,
// 		[name]: value,
// 	}));
// };

// const handleMallTypeChange = (e: React.ChangeEvent<HTMLSelectElement>, setFilter: ) => {
//     const { name, value } = e.target;
//     setFilters((prev) => ({
//         ...prev,
//         [name]: value,
//     }));
// };
